{
  "name": "merchant-portal",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/merchant-portal",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/merchant-portal",
        "outputPath": "dist/apps/merchant-portal"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/merchant-portal"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "merchant-portal:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "merchant-portal:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "merchant-portal:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "merchant-portal:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/merchant-portal/jest.config.ts",
        "passWithNoTests": true
      },
      "configurations": {
        "ci": {
          "ci": true,
          "codeCoverage": true
        }
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/merchant-portal/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
